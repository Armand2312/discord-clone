/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['discord-clone-assets.s3.us-east-1.amazonaws.com'], // Add your external domain here
      },
};

export default nextConfig;
